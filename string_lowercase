#include<iostream>
#include<string>
#include<cctype>
using namespace std;
int main(){
    string s="";
    cout<<"Enter a  string:";
    cin>>s;
    for(int i=0;i<s.length();i++){
        s[i]=tolower(s[i]);
    }

    cout<<"lower case string :"<<s;
    return 0;
}