#include<iostream>
#include<string>
using namespace std;
int main(){
    string r,s="";
    cout<<"Enter a string:";
    cin>>s;
    for (int i=s.length()-1;i>=0;i--){
        r+=s[i];
    }
    cout<<"Reverse of string:"<<r;
    return 0;
}