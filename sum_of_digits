#include <iostream>
using namespace std;
int main() {
    int a, b= 0;
    cout << "Enter number: ";
    cin >> a;

    while (a > 0) {
        b += a % 10; 
        a = a / 10; 
    }

    cout << "Sum of digits is " << b;
    return 0;
}