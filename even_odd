#include <iostream>
using namespace std;
int main() {
    int a;
    cout << "Enter number:";
    cin>>a;
    if(a%2==0){
       cout<<"number is even";
    }
    else{
        cout<<"numbers is odd ";
    }
    return 0;
}