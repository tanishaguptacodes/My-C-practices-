#include <iostream>
using namespace std;
int main() {
    int a;
    cout<<"enter a number:";
    cin>>a;
    if(a<=1){
       cout<<"not prime"; 
    }
    else{
        bool p=true;
        for (int i=2;i<a;i++){
            if(a%i==0){
                p=false;
                break;
            }
        }
        if (p){
            cout<<" prime"; 
        }
        else{
            cout<<"not prime"; 
        }
    }
    return 0;
}